/**
 * Package contains data files for filter transmission functions. The files must consist of two columns of numbers;
 * the first gives the wavelength in Angstroms, the second gives the filter transmission.
 */
package resources.filters;
